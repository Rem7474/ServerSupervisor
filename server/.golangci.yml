run:
  timeout: 5m
  tests: true
  skip-dirs:
    - vendor

linters:
  enable:
    - errcheck
    - ineffassign
    - staticcheck
    - govet
    - unused
    - gosimple
    - typecheck
  disable:
    - structcheck  # Deprecated
    - deadcode     # Deprecated
    - varcheck     # Deprecated

linters-settings:
  errcheck:
    check-blank: true
    check-type-assertions: true
    exclude-functions:
      - (io.Closer).Close
      - (*database/sql.Rows).Close
  
  govet:
    enable-all: true
    
  staticcheck:
    checks: ["all"]

issues:
  # Auto-fix ce qui peut l'être
  fix: true
  
  # Exclure certains fichiers de test
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gosec

  # Maximum issues per linter
  max-issues-per-linter: 0
  max-same-issues: 0

output:
  formats:
    - format: colored-line-number
  sort-results: true
