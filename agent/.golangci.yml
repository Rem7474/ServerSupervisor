version: 2

run:
  timeout: 5m
  tests: true
  skip-dirs:
    - vendor

linters:
  enable:
    - errcheck
    - ineffassign
    - staticcheck
    - govet
    - unused
    - gosimple
  disable:
    - structcheck  # Deprecated
    - deadcode     # Deprecated
    - varcheck     # Deprecated

linters-settings:
  errcheck:
    check-blank: true
    check-type-assertions: true
    exclude-functions:
      - (io.Closer).Close
      # Allow not checking errors for ReportCommandResult (logging function)
      - (*github.com/serversupervisor/agent/internal/sender.Sender).ReportCommandResult
  
  govet:
    enable-all: true
    
  staticcheck:
    checks: ["all"]

issues:
  # Auto-fix ce qui peut l'être
  fix: true
  
  # Exclure certains fichiers de test
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gosec
    # Exclude specific known issues that are intentional
    - linters:
        - errcheck
      text: "Error return value.*ReportCommandResult.*is not checked"
    - linters:
        - ineffassign
      text: "ineffectual assignment to.*state"
    - linters:
        - staticcheck
      text: "SA9003.*empty branch"

  # Maximum issues per linter
  max-issues-per-linter: 0
  max-same-issues: 0

output:
  format: colored-line-number
  sort-results: true
